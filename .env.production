# Kubernetes configuration
# For production - set to "false"
VITE_USE_MOCK_K8S_DATA=false

# Deployment environment
DEPLOYMENT=production

# Kubernetes API proxy settings
VITE_K8S_PROXY_URL=/kube-migrate
VITE_API_PROXY_URL=/kube-migrate

# AWS configuration
VITE_AWS_REGION=us-east-1

# AWS Cognito configuration
VITE_COGNITO_USER_POOL_ID=us-east-1_0phCrx0Ao
VITE_COGNITO_CLIENT_ID=4qfs9mvg1phde56htj0d3b3ku9
VITE_DYNAMODB_REGION=us-east-1

# Server configuration
PORT=3001
NODE_ENV=production

# Note: In production, AWS credentials are not set explicitly
# The application will use the IAM Role attached to the EC2 instance

# Optional: Path to kubeconfig file (can also be uploaded via UI)
# KUBECONFIG_PATH=/path/to/your/kubeconfig
